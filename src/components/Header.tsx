import { Layout } from "@stellar/design-system";

export const Header = () => (
  <Layout.Header
    projectTitle="AMM Reference UI"
    projectLink="https://stellar.org"
    hasDarkModeToggle
  />
);
